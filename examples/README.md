# Навчальні приклади Go

Приклади демонструють окремі техніки для лабораторних робіт № 2–4, але не є готовим каркасом жодного індивідуального варіанта. У них використано нейтральні предметні області: привітання, перетворення довжини, калібрувальні вимірювання й текстові звіти.

Усі каталоги входять до одного Go-модуля. Із каталогу `examples` виконайте:

```powershell
gofmt -w .
go test ./...
go vet ./...
```

## Приклади

- [`module-demo`](module-demo/) — локальний пакет і імпорт за module path.
- [`console-input`](console-input/) — читання двох рядків, parsing і валідація без залежності від `os.Stdin` у тестах.
- [`flag-subcommands`](flag-subcommands/) — підкоманди, `flag.FlagSet`, потоки та коди завершення.
- [`table-driven-tests`](table-driven-tests/) — табличні позитивні, негативні й граничні тести.
- [`clean-main`](clean-main/) — розділення `main`, предметної логіки та представлення.
- [`writer-output`](writer-output/) — формування результату через `io.Writer` і перевірка через `bytes.Buffer`.

- [`file-safety`](file-safety/) — межа байтів і відмова від перезапису.
- [`json-strict`](json-strict/) — перевірка одного JSON-документа та предметних правил.
- [`html-report`](html-report/) — автономний HTML із даних; запускайте з каталогу прикладу за його README.

Кожен приклад без зовнішніх файлів запускається з кореня цього модуля:

```powershell
go run ./module-demo
go run ./console-input
go run ./flag-subcommands length -value 250 -from cm -to m
go run ./table-driven-tests
go run ./clean-main -value 2.5 -from m -to cm
go run ./writer-output
```

Перед перенесенням ідеї до власної роботи поясніть, яку відповідальність має кожна функція та чому її контракт підходить вашій предметній області.

[До головної сторінки публічних матеріалів](../README.md)


## Необов’язкове поглиблення

- [Generics](generics-basics/) — параметри типів і множина.
- [JSONL](jsonl-records/) — окремі записи та номер помилкового рядка.
- [CSV](csv-records/) — лапки, коми та багаторядкові поля.
- [CSV і формули](csv-formula/) — межі текстової політики експорту.

Ці приклади не розширюють обов’язкові вимоги лабораторної № 4.

Перевірено 21.09.2026 на Windows у PowerShell з Go 1.25.1: `go test ./...`, `go vet ./...` і запуск нових прикладів. Команди Bash окремо не запускалися. HTML згенеровано; візуальний перегляд у браузері під час цієї перевірки не виконувався.
